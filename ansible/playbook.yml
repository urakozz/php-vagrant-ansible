---
- hosts: all
  sudo: true
  vars:
    sys_packages: ["nano"]
  roles:
    - init
    - php
    - composer
    - mongodb
    - {role: mysql, db: test, user: root, password: root}
    - {role: nginx, root: /vagrant/www, server_name: test.local}
